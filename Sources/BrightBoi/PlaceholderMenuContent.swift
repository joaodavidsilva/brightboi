import SwiftUI

/// Placeholder dropdown content. Ticket 04+ replaces this with the real slider UI.
struct PlaceholderMenuContent: View {
    var body: some View {
        VStack {
            Text("BrightBoi")
        }
        .padding()
        .frame(width: 200, height: 100)
    }
}
